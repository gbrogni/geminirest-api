# Gemini REST API

## Relevant technologies
- NestJS: Nest (NestJS) is a framework for building efficient and scalable Node.js server-side applications.
- Zod: Zod is a data validation library for TypeScript.
- Prisma: Prisma is a database persistence library for Node.js.

## Installation

Make sure you have Node.js in the version 16.20.2, Docker and npm installed on your machine.

1. Clone this repository:
   `git clone https://github.com/gbrogni/geminirest-api.git`

## Configuration

1. Rename the .env.example file to .env and configure the environment variables as needed.

## Usage

Run the following command `docker-compose up --build`

These are the following users the script `seed.js` created for you to use in the requisitions:

- 'customer-1'
- 'customer-2'
- 'customer-3'